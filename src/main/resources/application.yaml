spring:
  datasource:
    url: jdbc:sqlite:db.sqlite
    driver-class-name: org.sqlite.JDBC
#    username:
#    password:
# 하위개체 처럼 보임

mybatis:
  mapper-locations: "classpath:mybatis/mappers/*.xml"
  type-aliases-package: "com.lombok.lombok.model"
  configuration:
    map-underscore-to-camel-case: true # snake_case - camelCase, PascalCase